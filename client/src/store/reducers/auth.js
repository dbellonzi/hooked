const initialState = {
    isAdmin: false,
    userId: null,
    userName: null,
    token: null,
    error: null,
}

const reducer = (state=initialState, action) => {
    return state;
};

export default reducer;